steps:
# AMD Build
- name: 'gcr.io/cloud-builders/docker'
  id: 'amd64'
  args:
  - 'build'
  - '-f=docker/Dockerfile'
  - '--build-arg=arch=amd64'
  - '--tag=gcr.io/${PROJECT_ID}/panoptes-piaa:latest'
  - '.'
  waitFor: ['-']

# Push
- name: 'gcr.io/cloud-builders/docker'
  id: 'push-amd64'
  args:
  - 'push'
  - 'gcr.io/${PROJECT_ID}/panoptes-piaa:latest'
  waitFor: ['amd64']

images:
  - 'gcr.io/${PROJECT_ID}/panoptes-piaa:latest'
